name: Upload Build Scans

on:
  workflow_run:
    workflows: [ "Verify Build" ]
    types: [ completed ]

jobs:

  publish-build-scans:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      pull-requests: write
    steps:
      - name: Setup Build Scan link capture
        uses: gradle/develocity-actions/maven-setup@9a1d703f04a3255bbf4665f29aff1356e5dc366c
      - name: Publish Build Scans
        uses: gradle/develocity-actions/maven-publish-build-scan@9a1d703f04a3255bbf4665f29aff1356e5dc366c
        with:
          develocity-url: 'https://scans.gradle.com'
